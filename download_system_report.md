# 🛡️ VidFetch 视频嗅探下载与防盗链绕过技术报告

本报告详细阐述了本项目底层如何实现**零第三方 API 依赖**、**纯本地无头浏览器驱动**的音视频物理直链嗅探，以及如何通过后端代理技术粉碎大厂的防盗链与画质限制。

---

## 🛠️ 1. 核心架构与业务时序

整个下载流程分为 **第一阶段：直链嗅探（API `/api/parse`）** 和 **第二阶段：代理流式下载（API `/api/download`）**：

```
+------------------+         (1) 粘贴链接        +--------------------+
|  Vue 3 前端界面   | -------------------------> |  Puppeteer 浏览器  |
| (http://5173/)   | <------------------------- | (后台隐藏运行)     |
+------------------+     (2) 拦截并篡改 API      +--------------------+
         |                   获取 1080p 物理直链
         |
         | (3) 点击下载，请求后端代理接口
         v
+------------------+    (4) 动态伪造 UA/Referer  +--------------------+
|  Express 服务端  | -------------------------> |   大厂 CDN 服务器  |
| (http://3000/)   | <------------------------- |  (B站/抖音存储源)   |
+------------------+      (5) Pipe 管道流式透传  +--------------------+
```

---

## 🔬 2. 核心控制层解析与参数篡改 (直链嗅探)

在嗅探阶段，我们通过 Puppeteer 的网络底层拦截，强行修改网络请求参数，索要最高清的资源。

### A. Bilibili (B站) 接口劫持
* **拦截目标**：`/x/player/wbi/playurl` 或 `/x/player/playurl`。
* **参数篡改**：
  1. `fnval=0`：强制 B 站服务器下发**完整独立的 MP4 直链**，避开其默认采用的音视频分离式 DASH 编码流（避免了后续繁琐的 ffmpeg 音视频合成）。
  2. `qn=80`：强行将清晰度级别拉升至 **1080P 高清档位**。虽然免登录状态下 B 站会进行服务端权限 fallback，但这能确保系统拿到**非登录状态下的物理清晰度极限**（480P~720P 高码率），而非移动端默认的 360P 极速画质。

### B. Douyin (抖音) 去水印与 1080p 重构
* **拦截目标**：网页加载过程中，针对 `/aweme/v1/play/` 或带有 `video_id=` 的网络层请求。
* **黑客重构**：
  1. **提取 ID**：利用正则 `video_id=([a-zA-Z0-9_]+)` 捕获视频的底层物理哈希 ID。
  2. **强锁 1080p**：抛弃前端正在渲染的低码率数据，后端强制组装为：
     `https://aweme.snssdk.com/aweme/v1/play/?video_id=${videoId}&ratio=1080p`
  3. **早期决议**：一旦提取出 ID，Puppeteer 立即闭合，使解析过程从 15 秒压缩至 2~3 秒内，效率飞跃。

---

## 🔒 3. 核心代理技术与突破安全壁垒 (下载阶段)

当解析出物理直链后，前端通过动态创建隐式 `<a>` 标签，将链接传递给后端的 `/api/download`。后端服务通过四重欺骗，完美绕过安全封锁：

### 🧱 障壁 1：跨域资源共享限制 (CORS Block)
* **大厂防御**：抖音和 B 站的 CDN 存储服务器配置了严格的 `Access-Control-Allow-Origin` 头，浏览器直接请求会报跨域错误，阻断文件下载。
* **突破手段**：通过 Express 服务器在中途扮演**代理人**。由于 CORS 是**浏览器特有的沙箱策略**，Node.js 服务端发起 HTTP 请求完全不受跨域限制。

### 🧱 障壁 2：防盗链与 Referer 校验
* **大厂防御**：
  * **B站 CDN**：严查 `Referer`，若请求头非 `*.bilibili.com`，直接下发 `403 Forbidden`。
  * **抖音 CDN**：恰恰相反，若请求头带有第三方源（如 `localhost`），会直接拒绝连接。
* **突破手段**：在后端代理中使用 Axios 发起流式请求时，实行**差异化请求头注入**：
  * 解析为 B站 视频时，强行写入：`'Referer': 'https://www.bilibili.com'`
  * 解析为 抖音 视频时，**彻底删除**整个 `Referer` 属性，使其呈现安全的无来源直接访问。

### 🧱 障壁 3：设备指纹审查 (User-Agent 审查)
* **大厂防御**：抖音的无水印 `/play/` 重定向接口如果被识别为 PC 端普通浏览器（Chrome/Firefox）发起的直接下载，会返回 403 并强制触发滑动拼图验证码。
* **突破手段**：在代理端针对抖音请求，强行使用**苹果手机端 UA** 掩人耳目：
  `Mozilla/5.0 (iPhone; CPU iPhone OS 16_6 like Mac OS X) AppleWebKit/... Safari/...`
  使其服务器确信这是一个安全的移动端客户端网络播放行为，直接放行。

### 🧱 障壁 4：HTTP 302 临时重定向锁定
* **大厂防御**：抖音的无水印直链接口实际上是一个 **302 重定向调度接口**，会在几毫秒内将请求重映射到真实的 CDN 物理分发节点，且重定向后链接时效极短。
* **突破手段**：Axios 的 `stream` 模式能完整、无感地自动跟踪 HTTP 重定向。在代理内部自动处理所有的 302 跳转，确保最终建立的流连接指向最真实的底层文件节点，并使用 `response.data.pipe(res)` 将二进制字节以**低内存占用、高吞吐**的管道流传回前端浏览器。
