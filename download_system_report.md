# 🛡️ VidFetch 视频嗅探下载与系统底层安全技术报告

本报告详细阐述了本项目底层如何实现**零第三方 API 依赖**、**纯本地无头浏览器驱动**的音视频物理直链嗅探，以及如何通过后端代理技术绕过大厂的防盗链与画质限制。同时，报告记录了系统内置的账户安全和审计日志过滤架构。

---

## 🛠️ 1. 核心架构与业务时序

整个下载流程分为 **第一阶段：直链嗅探（API `/api/parse`）** 和 **第二阶段：代理流式下载（API `/api/download`）**：

```
+------------------+         (1) 粘贴链接        +--------------------+
|  Vue 3 前端界面   | -------------------------> |  Puppeteer 浏览器  |
| (http://5173/)   | <------------------------- | (后台隐藏运行)     |
+------------------+     (2) 拦截并篡改 API      +--------------------+
         |                   获取 1080p 物理直链
         |
         | (3) 点击下载，请求后端代理接口
         v
+------------------+    (4) 动态伪造 UA/Referer  +--------------------+
|  Express 服务端  | -------------------------> |   大厂 CDN 服务器  |
| (http://3000/)   | <------------------------- |  (B站/抖音存储源)   |
+------------------+      (5) Pipe 管道流式透传  +--------------------+
```

---

## 🔬 2. 核心控制层解析与参数篡改 (直链嗅探)

在嗅探阶段，我们通过 Puppeteer 的网络底层拦截，强行修改网络请求参数，索要最高清的资源。

### A. Bilibili (B站) 接口劫持
* **拦截目标**：`/x/player/wbi/playurl` 或 `/x/player/playurl`。
* **参数篡改**：
  1. `fnval=0`：强制 B 站服务器下发**完整独立的 MP4 直链**，避开其默认采用的音视频分离式 DASH 编码流（避免了后续繁琐的 ffmpeg 音视频合成）。
  2. `qn=80`：强行将清晰度级别拉升至 **1080P 高清档位**。虽然免登录状态下 B 站会进行服务端权限 fallback，但这能确保系统拿到**非登录状态下的物理清晰度极限**（480P~720P 高码率），而非移动端默认的 360P 极速画质。

### B. Douyin (抖音) 去水印与 1080p 重构
* **拦截目标**：网页加载过程中，针对 `/aweme/v1/play/` 或带有 `video_id=` 的网络层请求。
* **黑客重构**：
  1. **提取 ID**：利用正则 `video_id=([a-zA-Z0-9_]+)` 捕获视频的底层物理哈希 ID。
  2. **强锁 1080p**：抛弃前端正在渲染的低码率数据，后端强制组装为：
     `https://aweme.snssdk.com/aweme/v1/play/?video_id=${videoId}&ratio=1080p`
  3. **早期决议**：一旦提取出 ID，Puppeteer 立即闭合，使解析过程从 15 秒压缩至 2~3 秒内，效率飞跃。

---

## 🔒 3. 核心代理下载与流式透传技术

当解析出物理直链后，前端将链接传递给后端的 `/api/download`。后端服务通过多重机制安全保存视频：

### A. 跨域资源共享限制 (CORS Bypass)
* **安全绕过**：由于大厂的 CDN 服务器配置了严格的 `Access-Control-Allow-Origin` 头，浏览器直接请求会报跨域错误。我们通过 Node.js 后端代理人发起请求，彻底规避浏览器的 CORS 沙箱限制。

### B. 动态 Referer & User-Agent 欺骗
* **B站 CDN**：Axios 流式下载请求会动态添加：`'Referer': 'https://www.bilibili.com'` 头。
* **抖音 CDN**：彻底**擦除** `Referer`，并动态注入 iPhone 设备 `User-Agent`，伪装成安全的移动客户端本地播放行为，规避滑动拼图验证码校验。

### C. 管道化流式透传 (Memory Optimization)
* 后端使用 `response.data.pipe(res)` 将二进制字节以**低内存占用、高吞吐**的管道流传回前端浏览器，避免在服务器内存中缓存庞大视频文件，有效应对多并发。

---

## 🛡️ 4. 账户安全与审计日志过滤架构

为了满足企业级系统的安全性与审计需求，本项目实现了以下安全体系：

### A. 账户安全与隐私保护
* **邮箱验证码流控**：对发送验证码 API 实施双重限流：(1) 基于请求 IP 限流（每分钟最多 1 次）；(2) 基于邮箱地址限流（每分钟最多 1 次）。有效防御短信轰炸与邮件批量发送攻击。
* **数据脱敏展示**：对用户名（邮箱格式）在前端进行脱敏加密（展示前3位或前1位，结合后缀如 `123***@qq.com`），并且禁止在用户界面直接编辑邮箱账号。
* **修改密码与会话废弃**：不论是通过原密码修改，还是安全邮箱重置，密码修改成功后会**立即清除 db.json 中的对应会话（Sessions）**，强制全部活跃客户端下线重新登录，预防盗号残留。

### B. 管理员审计日志过滤与用户画像
* **多字段多条件筛选**：前端集成了一个 5 维度的日志筛选引擎，支持对操作时间起止区间、账号（用户名）、用户昵称、操作动作、提取链接进行模糊及精确的多条件联合筛选。
* **用户画像详情查看**：提供查看单个用户的详细状态，包括当前头像、角色权限、简介、以及今日已提取的额度监控。
* **快速筛选连动**：支持在用户详情页一键“查看历史日志”或“查看今日日志”，系统自动为操作日志填充对应的筛选字段（如当前用户名、当前本地日期）并跳转，实现快速审计追踪。
